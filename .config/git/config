[user]
	name = a5ob7r
	email = 12132068+a5ob7r@users.noreply.github.com
[color]
	ui = true
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[ghq]
	root = ~/src
[alias]
	co = checkout
	mg = merge --verbose
	f = fetch --verbose
	glog = log --pretty='format:%C(yellow)%h %C(reset)%s %C(green)%cr %C(cyan)[%an]%C(red)%d' --graph
	ph = push --verbose
	br = branch --verbose
	sw = switch
	c = commit
	tohead = switch -d origin/HEAD
	h = tohead
[core]
	commentChar = auto
	# NOTE: This is for staging/unstading files, which contains multibyte character in its path, on gina.vim.
	quotepath = false
[pull]
	ff = only
	rebase = false
[commit]
	verbose = true
[fetch]
	prune = true
[color "diff-highlight"]
	oldNormal = red bold
	oldHighlight = red bold 52
	newNormal = green bold
	newHighlight = green bold 22
[color "diff"]
	meta = yellow
	frag = magenta bold
	commit = yellow bold
	old = red bold
	new = green bold
	whitespace = red reverse
[diff-so-fancy]
	first-run = false
